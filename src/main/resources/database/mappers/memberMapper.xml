<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.mhj.s1.member.MemberDAO">
  
  	<!-- SELECT -->
  	<select id="getMemberList" resultType="MemberDTO">
  		SELECT ID, PW, NAME, PHONE, EMAIL
  		FROM BANKMEMBERS
  		ORDER BY ID DESC
  	</select>
  	
  	<select id="getMemberLogin" parameterType="MemberDTO" resultMap="getMemberRole">
  		SELECT *
  		FROM BANKMEMBERS
  			INNER JOIN
  			MEMBER_ROLE
  			USING (ID)
  			INNER JOIN
  			ROLE
  			USING (ROLENUM)
  		WHERE ID=#{id}
  	</select>
  	
  	<resultMap type="MemberDTO" id="getMemberRole">
  		<id column="ID" property="id"/>
  		<result column="PW" property="pw"/>
  		<result column="NAME" property="name"/>
  		<result column="PHONE" property="phone"/>
  		<result column="EMAIL" property="email"/>
  		<association property="roleDTOs" javaType="RoleDTO">
  			<id column="ROLENUM" property="roleNum"/>
  			<result column="ROLENAME" property="roleName"/>
  		</association>
  	</resultMap>
  	
  	<select id="getMemberPage" parameterType="MemberDTO" resultType="MemberDTO">
  		SELECT *
  		FROM BANKMEMBERS
  		WHERE ID=#{id} AND PW=#{pw}
  	</select>
  	
  	<!-- INSERT -->
  	<insert id="setMemberAdd" parameterType="MemberDTO">
  		INSERT INTO BANKMEMBERS
  		VALUES (#{id}, #{pw}, #{name}, #{phone}, #{email})
  	</insert>
  	
  	<insert id="setMemberRoleAdd" parameterType="MemberDTO">
  		INSERT INTO MEMBER_ROLE (ID, ROLENUM)
  		VALUES (#{id}, (SELECT ROLENUM FROM ROLE WHERE ROLENAME='MEMBER'))
  	</insert>
  	
  	<!-- UPDATE -->
  	<update id="setMemberUpdate" parameterType="MemberDTO">
  		UPDATE BANKMEMBERS
  		SET NAME=#{name}, PHONE=#{phone}, EMAIL=#{email}
  		WHERE ID=#{id}
  	</update>
  </mapper>